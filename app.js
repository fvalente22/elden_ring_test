/**
 * handleClick opens link in a new window
 */

const handleClick = () => {
    window.open(
      'https://calendly.com/trust-and-will/15min-trust-will-support',
      '_blank'
    );
  }
//   this was for the button testing but leaving here in case

/**
 * when clicked it will return value of X
 * @param {string} value 
 * @returns 
 */
const handleClick = (value) => {
    return value
  }
